# Data Model
Migration `025_legal_authorities_foundation.sql` defines sources, configurable types, jurisdictions, institutions, authorities, immutable versions, hierarchy, relationships, verification, licence controls and audits. Stable public identifiers are separate from primary keys; searchable governance fields remain relational.
