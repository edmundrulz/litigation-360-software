# Phase 12 Fixed Master Bootstrap

This file fixes the missing local file/folder problem.

## How to use

1. Download `PHASE-12-FIXED-MASTER-BOOTSTRAP.ps1`
2. Save it inside:
   `C:\Users\jep_edmundrulz\litigation-360-workspace\litigation-360-software`
3. Open PowerShell.
4. Run:

```powershell
cd "C:\Users\jep_edmundrulz\litigation-360-workspace\litigation-360-software"
powershell -ExecutionPolicy Bypass -File ".\PHASE-12-FIXED-MASTER-BOOTSTRAP.ps1"
```

It creates the missing `_LEOS_CONTROL` folders and the next script:
`PHASE-12.0C-READONLY-PROJECT-DISCOVERY.ps1`
