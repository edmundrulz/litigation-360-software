const sources=require("./legalAuthoritySources"); const taxonomy=require("./legalAuthorityTaxonomy");
describe("Legal Authorities reviewed seeds",()=>{ test("contains ten unique safe source records",()=>{ expect(sources).toHaveLength(10); expect(new Set(sources.map(item=>item.source_id)).size).toBe(10); sources.forEach(source=>{ expect(source.scraping_status).toBe("disabled"); expect(source.full_text_storage_allowed).toBe(0); expect(source.base_url).toMatch(/^https:\/\//); }); }); test("contains unique configurable authority types",()=>{ expect(taxonomy.length).toBeGreaterThanOrEqual(50); expect(new Set(taxonomy.map(item=>item.authority_type)).size).toBe(taxonomy.length); }); });
